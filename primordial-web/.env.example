# Base URL of the Primordial index service (catalog + ratings/stats API).
# Leave unset to build/dev against the bundled local mock fixture
# (src/fixtures/catalog.json). Set to the live URL to fetch real data.
#
# PRIMORDIAL_INDEX_URL=https://index.primordial.dev
